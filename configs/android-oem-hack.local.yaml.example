# Локальный оверлей для android-oem-hack.yaml (gitignore).
# Скопируй в android-oem-hack.local.yaml и правь.
# Merge через github.com/yaml/go-yaml-dom: maps рекурсивно, списки — replace.

flash:
  disk: disk4
  force: true
  verify: true
